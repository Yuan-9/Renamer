{
  "name": "renamer",
  "version": "0.1.0",
  "description": "Windows photo and video batch renaming tool.",
  "main": "src/main/index.js",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "vite": "vite --host 127.0.0.1",
    "electron": "electron .",
    "start": "electron .",
    "build": "vite build",
    "dist": "npm run build && electron-builder --config electron-builder.yml",
    "test": "vitest run"
  },
  "dependencies": {
    "exiftool-vendored": "^29.0.0",
    "lucide-react": "^0.475.0",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.0",
    "electron": "^41.0.0",
    "electron-builder": "^26.0.0",
    "vite": "^6.4.0",
    "vitest": "^3.2.0"
  }
}
